Need to copy sounds folder from this plugin into Sounds folder of your mod, should just drop in if you drop the folder into your mod root.

`loadSounds()` needs to be called in your devices `post_initialize()` or the sounds don't seem to load

TODO better docs for this